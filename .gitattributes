# Normalize to LF in repo and in working tree
* text=auto eol=lf

# Scripts that should use CRLF on Windows (override if needed)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary examples (uncomment if added later)
#*.png binary
#*.jpg binary
#*.zip binary

